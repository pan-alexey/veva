{
  "name": "@veva/core",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clear": "rimraf build",
    "dev": "tsc -w",
    "test": "jest --config jest.config.json",
    "lint": "eslint ./ --ext .js --ext .ts",
    "lint:fix": "eslint ./ --ext .js --ext .ts --fix"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@veva/eslint-config": "^1.0.0",
    "@veva/prettier-config": "^1.0.0",
    "@types/jest": "^26.0.20",
    "jest": "^26.6.3",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.5.3",
    "typescript": "^4.2.3"
  }
}
